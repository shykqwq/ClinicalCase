services:
  caddy:
    image: caddy:2-alpine
    container_name: clinical-case-caddy
    restart: unless-stopped
    env_file:
      - .env.production
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - clinical_case_caddy_data:/data
      - clinical_case_caddy_config:/config
    depends_on:
      api:
        condition: service_healthy

  postgres:
    image: postgres:16-alpine
    container_name: clinical-case-postgres
    restart: unless-stopped
    env_file:
      - .env.production
    environment:
      POSTGRES_DB: clinical_case
      POSTGRES_USER: clinical_case
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - clinical_case_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U clinical_case -d clinical_case"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
    container_name: clinical-case-api
    restart: unless-stopped
    env_file:
      - .env.production
    expose:
      - "3001"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3001/api/v1/health >/dev/null 2>&1 || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 10

volumes:
  clinical_case_postgres_data:
  clinical_case_caddy_data:
  clinical_case_caddy_config:
